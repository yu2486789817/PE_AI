server:
  address: 0.0.0.0
  port: 5001

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/se_project?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&characterEncoding=UTF-8
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:YUSHU750705}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Shanghai
  servlet:
    multipart:
      max-file-size: 2048MB
      max-request-size: 2048MB

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

# AI services
ai:
  yolo:
    base-url: http://localhost:8000
  video:
    base-url: http://localhost:8000
  chat:
    base-url: http://localhost:5000

# JWT
jwt:
  secret: PE_AI_MANAGER_SECRET_KEY_2025_CHANGE_IN_PRODUCTION
  expiration: 86400000  # 24 hours in ms

# File upload
file:
  upload-dir: ./uploads
