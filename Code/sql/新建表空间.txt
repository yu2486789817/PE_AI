-- MySQL 使用 InnoDB 存储引擎，每个表自动使用独立表空间（innodb_file_per_table=ON）
-- 无需手动创建表空间。Oracle 的 TABLESPACE 语法已移除。
--
-- 如需查看表空间使用情况：
--   SELECT table_schema, SUM(data_length + index_length) / 1024 / 1024 AS size_mb
--   FROM information_schema.tables WHERE table_schema = 'se_project' GROUP BY table_schema;
