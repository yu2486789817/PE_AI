# AIChat Dependencies
# AI Chat Assistant Service with Local LLM Support

# ================= Core Dependencies =================
# Web Framework
fastapi>=0.100.0
uvicorn[standard]>=0.23.0

# HTTP Requests
requests>=2.31.0

# ================= Local LLM Inference =================
# Transformers (model loading and inference)
transformers>=4.36.0

# PyTorch (already installed with CUDA support)
# torch>=2.0.0

# ================= LoRA Fine-tuning =================
# PEFT (Parameter-Efficient Fine-Tuning)
peft>=0.7.0

# Accelerate (distributed training support)
accelerate>=0.25.0

# Datasets (data processing)
datasets>=2.16.0

# BitsAndBytes (quantized training, saves VRAM)
bitsandbytes>=0.41.0

# ModelScope (model download, recommended for China)
modelscope>=1.10.0

# ================= Optional: vLLM (Linux only) =================
# vLLM provides high-performance inference but only works on Linux
# If you are on Linux, uncomment the following line:
# vllm>=0.3.0